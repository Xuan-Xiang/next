# next 学习笔记


## 路由


### 组件、目录

#### layout && template

`layout` 可以保留状态，`template` 不行，两者同时存在时 `layout`在`template`之上

#### 可自定义组件

1. `loading.tsx` 组件，通过`suspense`实现，组件含有异步状态时，显示`loading`组件

2. `error.tsx` 组件，通过`Error Boundary` 实现的。组件发生错误时显示该组件，必须为客户端组件

3. `not-found.tsx` 未匹配路由的组件到路由的组件，整个项目只需要一个。自定义后会覆盖默认的，该组件放在app下

#### 路由导航跳转

1. <Link>是一个内置组件，在a标签的基础上扩展了功能，并且还能用来实现预获取(prefetch)，prefetch只在生产模式下生效，默认为true，以及保持滚动位置(scroll)等。
[官方资料](https://nextjs.org/docs/pages/api-reference/components/link)

2. `useRouter` 可以在代码中根据逻辑跳转页面，例如根据用户权限跳转不同的页面。（**必须在客户端组件中,link组件却不需要**）

3. `redirect` 、`permanentRedirect`重定向，区别是 `permanentRedirect`为永久重定向

#### 动态路由

1. 使用`[xx]`为动态路由，`[...xx]` 为多个参数，`[[xx]]` 为可选参数

2. 两种获取参数方式，分为客户端啊组件与服务端组件
    * 客户端组件：useParams，必须声明客户端组件
    * 服务端组件：通过声明为异步函数的方式，通过函数入参拿到

#### 平行路由

平行路由指的是在同一布局 `layout.tsx`中 可以渲染多个页面

1. 使用`@xx `进行文件目录的声明，@符号并不会影响URL路径。在`layout.tsx`文件中传入对应的参数接受

#### 路由组

路由组也是一种基于文件夹的约定范式，可以让我们开发者，按类别或者团队组织路由模块，并且不影响 URL 路径。

1. 用法：使用 `(xx)` 包裹文件名，一般属于大型项目，需要将业务分开，比如后台管理 以及前台门户页面区分

2. 注意，使用时文件名不应该与其他文件名一样，档最终映射的URL一致时会报错


#### 接口定义

接口定义路径与页面路径一致，默认读取`route.ts`中方法，与`page.tsx` 用法一致，但同一目录下，不能同时存在`route.ts` 与 `page.tsx`。不然 `next` 不知道用哪一个了

1. `Next.js`是遵循`RESTful API`的规范，所以我们可以使用HTTP请求类型来定义请求

2. 我们在定义这些请求方法的时候不能修改方法名称而且必须是大写，否则无效。

3. `NextRequest` 与 `NextResponse` 本质上是对 `web Request Api` 的再次封装，跟 `fetch` 上的用法一致，只是做了更多的扩展。
    [fetchAPi具体用法](https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API)

4. 动态参数跟路由一致，可以通过 `[[...id]]` 的形式来使用，接受参数通过方法的第二个参数接受，跟异步服务端组件一致


### Proxy 代理

放在根目录下 `proxy.ts`，并导出异步函数`proxy`，会默认代理所有请求，包含静态资源、接口等


#### 配置代理

可以根据导出 `config` 对象来配置代理, 根据配置的`macher` 来表示匹配的路径，是一个数组，可以配置多个，并且可以根据对象形式。对象形式包含以下字段

- `source`: 表示匹配路径
- `has`: 表示匹配路径中必须(包含)某些条件
- `missing`: 表示匹配路径中(必须不包含)某些条件


### SSR、CSR、SSG、客户端组件、服务端组件、缓存组件

#### 基本概念

1. `SSR`（Server Side Rendering） 指的是在服务端完成所有数据的请求，比如一些接口，会在服务端内部请求好，然后放入到html中返回给客户端。客户端再根据加载js、css 实现页面的可交互以及样式

    1. 优点：更好的SEO以及首屏加载速度
    2. 缺点：服务端压力会比较大，每次请求都要请求数据，渲染拼接html结构


2. `CSR` 指的是如我们目前的SPA应用，往往会反回一个空的html页面，比如一个 id为app的div，之后再根据请求的js渲染dom。

    1. 优点：
        + 用户交互体验会很好，不用每次大的交互都要跳转页面
        + 组件化更容易维护，开发效率也比较高
    2. 缺点：首屏会比较慢


3. `SSG` （Static Site Generation） 静态站点生成。指的是一些不需要数据交互，或者说不需要每次根据用户来返回特定的页面，这样可以提前渲染好对应的HTML结构，放到服务端或者cdn中。每次用户请求都不需要服务端再进行一次渲染。

    1. 优点：相比于`SSR`的每次请求都要在服务端渲染，`SSG`相当于提前做好，用户请求时直接返回。服务端压力更小，响应更快(CDN直接承载请求)

    2. 缺点：不适用于动态数据（数据更新需要重新构建部署，如实时股价、实时评论）

4. `ISR`（Incremental Static Regeneration）增量静态再生成。 先用SSG方式提供静态页面，再一段时间后再次生成

#### next的组件概念

1. 客户端组件

2. 服务端组件

3. 组件缓存

Cache Components 是Next.js(16)版本特有的机制，实现了静态内容 动态内容 缓存内容的混合编排。保留了静态内容的加载速度，又具备动态渲染的灵活性，解决了静态内容(加载快但无法实时更新数据)和动态内容(加载慢但可以实时更新数据)权衡的问题。

+ 静态内容: 构建(npm run build)时进行预渲染，例如 「本地文件」「模块导入」「纯计算」（无网络请求、无用户相关数据）,会被直接编译成HTML瞬间加载、立即响应。

+ 动态内容：用户发起请求时才开始渲染的内容，依赖 “实时数据” 或 “用户个性化信息”，每次请求都可能生成不同结果，不会被缓存。例如「实时数据源」（如实时接口、数据库实时查询）或「用户请求上下文」（如 Cookie、请求头、URL 参数）。

+ 缓存内容：**缓存内容的本质就是缓存动态数据**，缓存之后会被纳入静态外壳(Static Shell),静态外壳就类似于毛坯房，会提前把结构搭建好，后续在通过(流式传输)填充里面的动态内容。

注意点：

next会默认缓存 fech请求的数据，若不想要缓存数据，可以使用`await connection()`。但并不是缓存组件，而是**防止重复请求/请求复用** (待确认，这里应该next 每次就是会缓存组件。跟cacheComponents效果一致)

开启`cacheComponents` 后会走缓存组件，此时异步操作必须经过`suspense`包裹, 之后可以使用 `use cache` 等进行缓存控制

如何退出缓存呢？

1.  `revalidate`属性可以设置缓存时间，单位为秒，可以设置为0s `export const revalidate = 0`

2. 使用 `export const dynamic = 'force-dynamic'` 强制组件为动态组件，不被缓存

3. 使用 接口`headers: { 'cache': 'no-store' }` 强制组件不被缓存

4. 使用任意以下几种 都不会缓存
    + cookies
    + headers
    + connection
    + searchParams
    + fetch和{ cache: ‘no-store’ }



### 注意事项

1. 客户端组件无法使用 async 异步方法